import { IconType } from "react-icons";

export interface BlogProps {
  title?: string;
  dateUp?: String;
  desc?: String;
  date?: String;
  look?: String;
  srcImage?: String;
  small: Boolean;
  // icon: IconType;
}
